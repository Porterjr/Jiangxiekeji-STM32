# 第9章 USART串口

## 9-1 USART串口协议

### 通信接口

- **通信的目的**：将一个设备的数据传送到另一个设备，扩展硬件系统
- **通信协议**：制定通信的规则，双方按照规则进行数据收发

### 常见通信接口对比

| 接口 | 引脚 | 双工 | 时钟 | 电平 | 设备 |
|------|------|------|------|------|------|
| **USART（串口）** | TX（数据发送脚）、RX（数据接收脚） | 全双工 | 异步 | 单端 | 点对点 |
| **I2C** | SCL（时钟）、SDA（数据） | 半双工 | 同步 | 单端 | 多设备 |
| **SPI** | SCLK（时钟）、MOSI（主机输出数据脚）、MISO（主机输入数据脚）、CS（片选） | 全双工 | 同步 | 单端 | 多设备 |
| **CAN** | CAN_H、CAN_L（差分数据脚） | 半双工 | 异步 | 差分 | 多设备 |
| **USB** | DP、DM（差分数据脚） | 半双工 | 异步 | 差分 | 点对点 |

### 串口参数及时序

- **波特率**：串口通信的速率。例：双方规定波特率为 1000bps → 1s 发 1000 位，1 位 1ms
- **起始位**：标志一个数据帧的开始，固定为低电平
- **数据位**：数据帧的有效载荷，1 为高电平，0 为低电平，低位先行
- **校验位**：用于数据验证，根据数据位计算得来
- **停止位**：用于数据帧间隔，固定为高电平
1帧共10位：
空闲 ─┬─ 起始位 ─┬─ 数据位8位(D0~D7) ─┬─ 停止位 ─┬─ 空闲
│   低电平  │   LSB → MSB        │   高电平  │
1帧共11位（带校验）：
空闲 ─┬─ 起始位 ─┬─ 数据位9位(D0~D8) ─┬─ 校验位(奇/偶) ─┬─ 停止位 ─┬─ 空闲


## 9-2 USART串口外设

### USART 通用同步/异步收发器

- 是 STM32 内部集成硬件外设，分为发送和接收两部分
  - **发送**：将数据寄存器的一个字节数据自动转换为协议规定的波形从 TX 发送出去
  - **接收**：自动接收 RX 引脚的波形按照协议规定，解码为一个字节数据存放在数据寄存器里
- 自带波特率发生器，最高达 4.5Mbits/s（本质分频器）
- 可配置数据位长度（8/9）、停止位长度（0.5/1/1.5/2）
- 可选校验位：无校验 / 奇校验 / 偶校验
- 支持同步模式、硬件流控制、DMA、智能卡、IrDA、LIN
- STM32F103C8T6 USART 资源：USART1、USART2、USART3

### USART 基本结构
PCLK2/1 → 波特率发生器 ─┬─→ 发送控制器 ─→ 发送数据寄存器 TDR ─→ 发送移位寄存器 ─→ GPIO(TX)
│
└─→ 接收控制器 ←─ 接收移位寄存器 ←─ 接收数据寄存器 RDR ←─ GPIO(RX)
↑
开关控制

### 波特率计算
波特率 = f_PCLK2/1 / (16 * DIV)

### USART 库函数

```c
USART_ClockInit();        // 配置同步时钟输出
USART_ClockStructInit();  // 同步时钟结构体初始化
USART_DMACmd();           // 开启 USART 到 DMA 的触发通道
USART_ReceiveData();      // 接收数据（读 DR 寄存器）
USART_SendData();         // 发送数据（写 DR 寄存器）
```
## 9-3 串口发送 & 串口接收
## 9-4 串口收发HEX数据包 & 串口收发文本数据包
HEX 数据包接收
数据格式：0xFF | 0x01 | 0x02 | 0x03 | 0x04 | 0xFE | 0xFF | 0x05 | 0x06 | 0x07 | 0x08 | 0xFE | ...
状态机：
S=0 等待包头 ──收到0xFF──→ S=1 接收数据 ──收够4个数据──→ S=2 等待包尾
    ↑_________________________________________收到0xFE_____|
    └────────────────────收到其他数据───────────────────────┘
              ↑______________________________收到其他数据____|
              └────────────────收到其他数据──────────────────┘
文本数据包接收
数据格式：'@' | 'A' | 'B' | 'C' | '\r' | '\n' | '@' | 'D' | 'E' | '\r' | '\n' | ...
状态机：
S=0 等待包头 ──收到'@'──→ S=1 接收数据/等待包尾 ──收到'\r'──→ S=2 等待包尾
    ↑_________________________________________收到'\n'______|
    └────────────────────收到其他数据───────────────────────┘
              ↑_________________________________收到其他数据|
              └────────────────收到其他数据──────────────────┘
## 实验项目
9-1 串口发送
9-2 串口接收
9-3 串口收发HEX数据包
9-4 串口收发文本数据包
实验记录
- 📷 [9-4 串口收发文本数据包实验GIF](notes/9-4 串口收发文本数据包实验.jpg)

---

[← 返回根目录](../README.md)
